{
  "name": "sdk",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/Looksy-tech/SDK#readme",
  "bugs": {
    "url": "https://github.com/Looksy-tech/SDK/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Looksy-tech/SDK.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "terser script/script.js -o script/script.min.js -c -m",
    "regression:capture": "playwright test -c regression-tests/playwright.config.js --project=capture",
    "regression:test": "playwright test -c regression-tests/playwright.config.js --project=regression",
    "regression:update": "playwright test -c regression-tests/playwright.config.js --project=regression --update-snapshots",
    "regression:ui": "playwright test -c regression-tests/playwright.config.js --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "terser": "^5.37.0"
  }
}
